Open bike geometry database with search and comparison tool on top.

# Development notes
In order to assemble all csv files together into a build/database.csv file one needs to run
```shell
uv run bgc
```